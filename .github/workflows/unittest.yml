name: unittest
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: Install Eigen3
        run: sudo apt update && sudo apt install libeigen3-dev
      - name: cmake
        run: mkdir build && cd build && cmake ..
      - name: make
        run: cd build && make
      - name: ctest
        run: cd build && ctest -V --rerun-failed --output-on-failure
